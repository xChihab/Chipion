<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Text?>
<?import javafx.scene.effect.DropShadow?>

<VBox alignment="CENTER" spacing="30" xmlns:fx="http://javafx.com/fxml" fx:controller="com.example.chipion1.ModeSelectionController"
      style="-fx-background-color: linear-gradient(to bottom, #1a1a2e, #16213e);">
    <padding>
        <Insets top="50" right="50" bottom="50" left="50"/>
    </padding>

    <!-- Logo et Titre -->
    <VBox alignment="CENTER" spacing="10">
        <Text text="MORPION" style="-fx-font-size: 72px; -fx-font-weight: bold; -fx-fill: #e94560; -fx-effect: dropshadow(three-pass-box, #e94560, 20, 0, 0, 0);"/>
        <Text text="Sonic Edition" style="-fx-font-size: 32px; -fx-font-weight: bold; -fx-fill: #0f3460; -fx-effect: dropshadow(three-pass-box, #0f3460, 10, 0, 0, 0);"/>
    </VBox>

    <!-- Menu Principal -->
    <VBox spacing="25" alignment="CENTER" style="-fx-background-color: rgba(22, 33, 62, 0.8); -fx-padding: 40; -fx-background-radius: 20;">
        <Label text="Choisissez votre mode de jeu" style="-fx-font-size: 24px; -fx-text-fill: #e94560; -fx-font-weight: bold; -fx-effect: dropshadow(three-pass-box, #e94560, 5, 0, 0, 0);"/>
        
        <VBox spacing="15" alignment="CENTER">
            <Button fx:id="classicModeButton" text="🎮 Mode Classique" onAction="#handleClassicMode" styleClass="mode-button"/>
            <Button text="⏱️ Mode Contre la montre" onAction="#handleTimeMode" styleClass="mode-button"/>
        </VBox>
    </VBox>

    <!-- Boutons d'action -->
    <HBox spacing="20" alignment="CENTER">
        <Button text="📖 Règles" onAction="#handleRules" styleClass="info-button"/>
        <Button text="🚪 Quitter" onAction="#handleQuit" styleClass="quit-button"/>
    </HBox>
</VBox>
